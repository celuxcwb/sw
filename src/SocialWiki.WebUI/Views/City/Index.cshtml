@model IEnumerable<SocialWiki.WebUI.Models.City>

@{
    ViewBag.Title = "Listar categorias";
    Layout = "~/Views/Shared/_Material.cshtml";
}


<div class="row">
    @*<div class="span3 bs-docs-sidebar">
        <ul class="nav nav-list bs-docs-sidenav affix">
            <li><a href="#dropdowns"><i class="icon-chevron-right"></i>Dropdowns</a></li>
            <li><a href="#buttonGroups"><i class="icon-chevron-right"></i>Button groups</a></li>
            <li><a href="#buttonDropdowns"><i class="icon-chevron-right"></i>Button dropdowns</a></li>
            <li><a href="#navs"><i class="icon-chevron-right"></i>Navs</a></li>
        </ul>

    </div>*@
    <div class="span9">
        <div class="bs-docs-example">
            <h2>Listar Categorias</h2>

            <p>
                @Html.ActionLink("Novo", "Create","City")
            </p>
            <table class="table">
                <thead>
                <th>
                    @Html.DisplayNameFor(model => model.Id.Pid)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UF)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IBGE)
                </th>
                <th></th>
                </thead>
                <tbody>



                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Id.Pid)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.UF)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IBGE)
                            </td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                                @Html.ActionLink("Visualizar", "Details", new { id = item.Id }) |
                                @Html.ActionLink("Excluir", "Delete", new { id = item.Id })
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>
